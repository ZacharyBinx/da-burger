
<li>
	{{name}}

	<button class="change-state" data-id="{{id}}" data-newburger="{{eaten}}">
		{{#if eaten}}NOM!{{else}}unNom?!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Yeet</button>
</li>
